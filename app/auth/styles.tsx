import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  background: { flex: 1, backgroundColor: "#634DBF", height: "100%" },
  animationWrapper: {
    marginTop: 60,
    width: "100%",
    alignItems: "center",
    justifyContent: "center",
    height: 300,
    marginVertical: 30,
  },
});

export default styles;
